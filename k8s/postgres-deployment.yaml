apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-deployment
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - env:
            - name: PG_DATA
              value: /var/lib/postgresql/pgdata
            - name: POSTGRES_DB
              value: dbname
            - name: POSTGRES_PASSWORD
              value: password
            - name: POSTGRES_USER
              value: username
            - name: TZ
              value: UTC
          image: postgres:15.0
          name: exminds-core-postgres
          ports:
            - containerPort: 5432
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: pgdata
              subPath: postgres
      restartPolicy: Always
      volumes:
        - name: pgdata
          persistentVolumeClaim:
            claimName: pgdata
